# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem
# …but DO track the expo-updates code-signing certificate (PUBLIC cert, no
# private key). It ships inside every installed APK and is required for anyone
# to reproduce the release build byte-for-byte. The signing PRIVATE key lives in
# EAS, never in the repo.
!certs/certificate.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Android signing keystores (never commit)
*.keystore

# expo-updates OTA code-signing PRIVATE key (never commit)
keys/

# Emulator test artifacts (screenshots, UI dumps, seeded test images)
emu-*.png
d2-home.png
testimg/
